import React from 'react';
// TODO spinner needs to updated here
const Spinner = ({}) => (
  <div
    style={{
      height: 300,
      width: '100vw',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    }}
  >
    loading...
  </div>
);

export default Spinner;
