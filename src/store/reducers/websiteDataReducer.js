import * as actionTypes from "../actions/types";

const initialState = {
  data: []
};

const websiteDataReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default websiteDataReducer;
